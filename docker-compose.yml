services:
  frontend:
    build: .
    container_name: heartmind_frontend

    ports:
      - "4200:4200"

    volumes:
      - .:/app
      - /app/node_modules

    stdin_open: true
    tty: true
    